def env = new Properties()
def envFile = rootProject.file('../.env')
if (envFile.exists()) {
    env.load(envFile.newDataInputStream())
}

// Set properties for direct access
env.each { key, value ->
    rootProject.ext[key] = value
}